{
  "name": "CopyRight-Free",
  "version": "1.0.0",
  "description": "Your project description here",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "node index.js",
    "build": "tsc --build || echo 'No tsconfig.json found, skipping build'",
    "typecheck": "tsc --noEmit || echo 'No tsconfig.json found, skipping typecheck'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.100.0",
    "nodemailer": "^6.10.1"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "prettier": "^3.8.1"
  }
}
